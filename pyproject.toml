[project]
name = "tidal-tool-query-lab"
version = "0.1.0"
description = "Package a Python local lab for query analysis with bounded scenario files, conflict explanations, and documented operating limits."
requires-python = ">=3.11"
